name: msmtpd

services:
  msmtpd:
    image: ${MSMTPD_IMAGE:-crazymax/msmtpd}
    container_name: ${MSMTPD_CONTAINER:-msmtpd}
    ports:
      - target: 2525
        published: 2500
        protocol: tcp
    environment:
      - "TZ=Europe/Paris"
      - "PUID=1500"
      - "PGID=1500"
      - "LISTEN_PORT=2525"
      - "SMTP_HOST=smtp.foo.bar"
    restart: always
